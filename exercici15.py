def calcular_longitud(y):
    #El que feim aqui es calcularf la llista i despres retornarla amb un len
    return len(y)
lista = [1, 99,44,55,77]

#El que feim aqui es calcular la longitud i la igualam a calcular la llista, i despres de fer això imprimim el resultat pero antes que que passar per la funció
longitud_de_lista = calcular_longitud(lista)
print(f"La longitud de la lista es: {longitud_de_lista} ")